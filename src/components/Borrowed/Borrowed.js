import { View, Text } from 'react-native';
import styles from './styles';

export default function Borrowed() {
    return (
        <View style={styles.container}>
            <Text>Borrowed page</Text>
        </View>
    );
}
